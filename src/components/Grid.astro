<div class='grid'>
  <slot />
</div>

<style>
  .grid {
    width: 100%;
    margin-top: 2.5em;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 1em;
  }
</style>
