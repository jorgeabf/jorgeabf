<div role="list" class="grid">
    <slot />
</div>

<style>
    .grid {
        max-width: 100%;
        margin: 0;
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
        gap: 1em;
    }
</style>