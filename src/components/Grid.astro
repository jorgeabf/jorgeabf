<div class='grid'>
  <slot />
</div>

<style>
  .grid {
    width: 100%;
    margin-top: 2.5em;
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 1em;
  }
</style>
